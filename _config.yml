# generated by course-infra 0.1.0 from the build repo — do not edit here; the next release overwrites this file
title: "DS2"
description: "ECBS5294 — Working with Data"
remote_theme: just-the-docs/just-the-docs@v0.10.0
color_scheme: light

search_enabled: true

aux_links:
  "GitHub Repo": "https://github.com/earino/ecbs5294-2026"

footer_content: "ECBS5294 &mdash; Central European University &mdash; 2026–2027"

back_to_top: true
back_to_top_text: "Back to top"

# Not rendered as pages at all.
exclude:
  - README.md
  - LICENSE.md

# Content directories are served (slides.html, PDFs, images) but never appear
# in the sidebar. One scope per directory covers every unit — nothing to
# enumerate when a unit is added.
defaults:
  - scope:
      path: "units"
    values:
      nav_exclude: true
  - scope:
      path: "assessments"
    values:
      nav_exclude: true
